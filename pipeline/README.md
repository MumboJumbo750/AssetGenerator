# Pipeline assets (templates, configs)

This folder will contain pipeline resources that are treated like code:

- ComfyUI workflow templates
- template bindings/mappings
- (later) export profile templates

Docs:

- `docs/pipeline/comfyui.md`
