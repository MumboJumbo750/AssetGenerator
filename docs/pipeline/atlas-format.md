# Atlas metadata format

The atlas format should be stable and engine-agnostic.

## Requirements

- frame rectangles (x/y/w/h)
- source size before trim
- pivot/origin per frame (optional but recommended)
- stable IDs that link back to assets/specs

## Schema

See `schemas/atlas.schema.json`.
