const target = process.argv[2] ?? "component";

console.log(`${target} is not scaffolded yet.`);
console.log("This repo currently contains docs + schemas + tooling scripts.");
console.log("Next milestone: add apps/backend apps/frontend apps/worker and wire npm scripts here.");

